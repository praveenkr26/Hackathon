{
  "name": "yojanasetu-monorepo",
  "version": "1.0.0",
  "description": "Monorepo configuration for YojanaSetu deployment on Render",
  "main": "backend/server.js",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "build": "echo 'Build handled by postinstall'",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
